import { ChallengesPipe } from './challenges.pipe';

describe('ChallengesPipe', () => {
  it('create an instance', () => {
    const pipe = new ChallengesPipe();
    expect(pipe).toBeTruthy();
  });
});
