import { ReservacionesPipe } from './reservaciones.pipe';

describe('ReservacionesPipe', () => {
  it('create an instance', () => {
    const pipe = new ReservacionesPipe();
    expect(pipe).toBeTruthy();
  });
});
